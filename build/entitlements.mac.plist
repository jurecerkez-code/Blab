<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- The hardened runtime is on, and under it the microphone is closed unless
       the app says out loud that it wants one. Without this key macOS does not
       deny Blab, it refuses to let Blab ask: no prompt, and no entry in the
       microphone list for anyone to switch on. -->
  <key>com.apple.security.device.audio-input</key>
  <true/>

  <!-- V8 writes machine code at runtime, which the hardened runtime otherwise
       forbids. Electron does not start without these three. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
